#pragma once
/*
This class will hold all high scores for the game in a hash table.
This allows for a linked list that is searchable, movable, and dynamic
to avoid errors with a static array.
*/
class ScoreAll
{
public:
	ScoreAll();
	~ScoreAll();
};